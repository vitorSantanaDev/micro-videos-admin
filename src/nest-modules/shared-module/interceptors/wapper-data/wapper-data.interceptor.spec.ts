import { WapperDataInterceptor } from './wapper-data.interceptor';

describe('WapperDataInterceptor', () => {
  it('should be defined', () => {
    expect(new WapperDataInterceptor()).toBeDefined();
  });
});
